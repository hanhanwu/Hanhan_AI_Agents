# === LLM & LangChain core ===
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-community>=0.3.0

# === LangGraph ===
langgraph>=0.2.0
langgraph-checkpoint-postgres>=0.1.0

# === Supabase (memory + vector store) ===
supabase>=2.4.0
vecs>=0.4.0                            # pgvector helper (optional, supabase SDK covers most cases)

# === Postgres driver (psycopg3 — required by LangGraph checkpointer) ===
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0

# === Embeddings ===
langchain-openai>=0.1.0                # OpenAI embeddings (text-embedding-3-small)
# langchain-huggingface>=0.0.3         # alternative: local HuggingFace embeddings

# === API server ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.3.0

# === Observability ===
langsmith>=0.1.0

# === Utilities ===
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.5.0

# === Dev / test ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
